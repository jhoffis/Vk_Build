#include "Component.h"
#include "vk/shading/gra_texture.h"

//void ImageComp::setup() {
//    auto img = Texture::loadImage(textureName.c_str());
//    texImageView = Texture::createTexture(img);
//    w = static_cast<float>(img.w);
//    h = static_cast<float>(img.h);
//}
